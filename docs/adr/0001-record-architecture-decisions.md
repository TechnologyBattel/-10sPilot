# 1. Record architecture decisions

- Status: accepted

## Context

The project spans a TypeScript frontend, a Python backend and shared packages. Decisions need a
durable, reviewable home.

## Decision

Use lightweight ADRs in `docs/adr`, numbered sequentially.

## Consequences

Every non-obvious structural decision gets a short document referenced from pull requests.
