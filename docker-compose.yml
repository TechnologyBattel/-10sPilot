services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tenspilot
    ports:
      - "5432:5432"
    volumes:
      - tenspilot_pgdata:/var/lib/postgresql/data

volumes:
  tenspilot_pgdata:
